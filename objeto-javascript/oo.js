// Código não executável!!

// Procedural
processamento(valor1,valor2,valor3)

// Orientação a Objetos
objeto={
    valor1,
    valor2,
    valor3,
    processamento(){
        // ...
    }
}

objeto.processamento() // Foco passou a ser o objeto

// Princípio importantes:
// 1. Abstração
// 2. Encapsulamento
// 3. Herança (prototype) - princípio de relação "é um"
// 4. Polimorfismo



/* Formas de se reutilizar código podem ser do tipo "tem um", como por exemplo um carro(objeto) possui um
motor(que também pode ser um objeto) fazendo com que não seja necessário especificar todos os dados de motor
em carro, pois já foram especificados no objeto motor.

    Outra forma de se reutilizar é pelo princípio do "é um", como uma subclasse é de uma classe, por exemplo
um fusca "é um" carro, ou seja, possui suas caractéristicas e métodos mas também herda as características e métodos
de carro, fazendo desnecessário refazer toda essa parte do código para o fusca. */

/* A relação de composição ("tem um") deve ser priorizada em relação à herança ("é um") */

